# Copy to ~/.cortexops/cursor-webhook.env (chmod 600)
# Get URL + token from cursor.com/automations → your daily automation → Webhook trigger

CURSOR_AUTOMATION_WEBHOOK_URL=https://api2.cursor.sh/automations/webhook/YOUR_AUTOMATION_ID
CURSOR_AUTOMATION_WEBHOOK_TOKEN=crsr_YOUR_TOKEN
